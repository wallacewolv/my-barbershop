import { TranslocoGlobalConfig } from '@jsverse/transloco-utils';

const config: TranslocoGlobalConfig = {
  rootTranslationsPath: 'apps/my-barbershop/public/i18n',
  langs: ['pt-BR', 'en'],
  keysManager: {},
};

export default config;
