import { DownloadStoragePipe } from './download-storage.pipe';

describe('DownloadStoragePipe', () => {
  it('create an instance', () => {
    const pipe = new DownloadStoragePipe();
    expect(pipe).toBeTruthy();
  });
});
